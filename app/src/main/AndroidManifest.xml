<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookie.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="app.cookie.app._new_architecture.dependency.App">
        <activity android:name="app.cookie.app._new_architecture.view.recipeslist.RecipeListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="app.cookie.app._new_architecture.view.recipedetails.RecipeDetailsActivity"
            android:parentActivityName="app.cookie.app._new_architecture.view.recipeslist.RecipeListActivity" />
        <activity android:name="app.cookie.app.view.stepdetails.StepDetailsActivity"
            android:parentActivityName="app.cookie.app._new_architecture.view.recipedetails.RecipeDetailsActivity"></activity>
    </application>

</manifest>